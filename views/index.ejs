<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyMeet - כניסה מהירה</title>
    <style>
        body { font-family: sans-serif; text-align: center; background: #202124; color: white; display: flex; align-items: center; justify-content: center; height: 100vh; margin: 0; }
        .container { background: #3c4043; padding: 40px; border-radius: 10px; width: 90%; max-width: 400px; box-shadow: 0 4px 10px rgba(0,0,0,0.5); }
        input { width: 100%; padding: 15px; margin: 10px 0; border: none; border-radius: 5px; box-sizing: border-box; font-size: 16px; }
        .btn { width: 100%; padding: 15px; margin-top: 10px; border: none; border-radius: 5px; cursor: pointer; font-size: 18px; font-weight: bold; color: white; }
        .btn-start { background: #1a73e8; }
        .btn-schedule { background: #5f6368; }
        h1 { margin-bottom: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>📹 MyMeet</h1>
        <p>הכנס שם כדי להתחיל</p>
        
        <form action="/join" method="POST">
            <input type="text" name="name" placeholder="השם שלך" required>
            <input type="hidden" name="roomId" value="<%= roomId %>">
            
            <button type="submit" name="action" value="start" class="btn btn-start">התחל שיחה עכשיו</button>
            <button type="submit" name="action" value="schedule" class="btn btn-schedule">🔗 צור קישור לאחר כך</button>
        </form>
    </div>
</body>
</html>
